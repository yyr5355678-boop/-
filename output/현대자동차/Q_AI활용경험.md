# 현대자동차 AI 활용 경험 — AI 아키텍처 전환으로 달성한 예측 오차 55% 개선

## 500자 버전 (실제 글자수: 497자)

[AI 아키텍처 전환으로 달성한 예측 오차 55% 개선]

LG Aimers 7기 해커톤에서 리조트 F&B 수요 예측 모델을 개발하며 AI를 직접 활용했습니다. 베이스라인 LSTM 코드는 메뉴 수백 개 각각에 독립 모델을 학습시켜 한 번에 3시간 반이 소요됐고, 하루 실험이 2~3회에 불과한 구조였습니다.

저는 LSTM이 메뉴 간 공통 패턴을 공유하지 못하는 구조적 한계를 파악하고, 데이터를 정형 피처로 변환해 단일 글로벌 LightGBM 모델로 전환했습니다. 학습 시간이 3시간 반에서 2분으로 단축돼 40회 이상의 실험이 가능해졌습니다. 또한 파편화된 테스트 파일을 개별 정규화하면 비수기·성수기의 절대 규모 차이가 소실되는 스케일 왜곡을 발견해, train 전 기간 기준 글로벌 스케일러를 구축해 해결했습니다.

그 결과 RMSE 18.45에서 8.12로 오차를 55.9% 개선했고, 수백 팀 중 상위 3.8%로 마무리했습니다. 파편화 데이터의 구조적 문제를 재설계하는 이 역량을, 공정 이상 감지와 부품 수요 예측에 직접 활용할 수 있습니다.

---

## 700자 버전 (실제 글자수: 709자)

[AI 아키텍처 전환으로 달성한 예측 오차 55% 개선]

LG Aimers 7기 해커톤에서 리조트 F&B 수요 예측 모델을 개발하며 AI를 직접 활용했습니다. 베이스라인 LSTM 코드는 메뉴 수백 개 각각에 독립 모델을 학습시켜 한 번에 3시간 반이 소요됐고, 하루 실험이 2~3회에 불과한 구조였습니다. 팀 내에서 검증을 줄이고 빠르게 제출하자는 의견과 파라미터를 정교하게 조정하자는 의견이 맞섰고, 저는 아키텍처 자체를 바꾸는 방향을 주도했습니다.

팀의 목표는 메뉴별 미래 7일 매출 수량을 정확히 예측하는 것이었으며, 저는 모델 설계와 데이터 전처리를 담당했습니다. 실제로 두 차례 기술적 병목이 발생했고, 두 문제 모두 데이터 구조를 근본적으로 재설계해 해결했습니다.

첫 번째는 연산 병목이었습니다. LSTM의 구조적 한계를 파악하고 데이터를 정형 피처로 변환해 단일 글로벌 LightGBM·CatBoost 모델로 전환했습니다. 학습 시간이 3시간 반에서 2분으로 단축됐고, 40회 이상의 실험이 가능해졌습니다. 두 번째는 스케일 왜곡이었습니다. 테스트 파일 10개를 개별 정규화하면 비수기·성수기의 절대 규모 차이가 소실됩니다. train 전 기간 기준 글로벌 스케일러를 구축해 모델이 체급 차이를 왜곡 없이 학습하도록 했습니다.

그 결과 RMSE를 18.45에서 8.12로 줄여 55.9% 개선했고, 상위 3.8%로 마무리했습니다. 이 전처리·모델링 역량을 공정 이상 감지와 부품 수요 예측에 직접 활용할 수 있습니다.

---

## 1000자 버전 (실제 글자수: 1011자)

[AI 아키텍처 전환으로 달성한 예측 오차 55% 개선]

LG Aimers 7기 해커톤에서 리조트 F&B 수요 예측 모델을 개발하며 AI를 직접 활용했습니다. 베이스라인 LSTM 코드는 메뉴 수백 개 각각에 독립 모델을 학습시켜 한 번에 3시간 반이 소요됐고, 하루 실험이 2~3회에 불과한 구조였습니다. 팀 내에서는 검증을 줄이고 빠르게 제출하자는 의견과 파라미터를 정교하게 조정하자는 의견이 맞섰으며, 어느 방향으로도 근본 문제가 해결되지 않아 압박감이 컸습니다.

팀의 목표는 메뉴별 미래 7일 매출 수량을 정확히 예측하는 것이었으며, 저는 모델 아키텍처 설계와 데이터 전처리를 주담당했습니다. 구조적 문제를 파악하고 파이프라인 전체를 재설계하는 방향을 주도하며 두 차례의 기술적 병목을 해결했습니다.

첫 번째는 연산 병목 문제였습니다. LSTM은 메뉴별 시계열 흐름은 포착하지만 리조트 전체를 관통하는 공통 패턴을 메뉴 간에 공유하지 못하는 구조적 한계가 있었습니다. 저는 데이터를 정형 피처로 변환해 단일 글로벌 LightGBM·CatBoost 모델로 전환하고, 직전 7일 평균 판매량·Lag 피처·변동성과 미래 날짜의 요일·공휴일 스코어를 결합한 뒤 Optuna로 파라미터를 최적화했습니다. 학습 시간이 3시간 반에서 2분으로 단축됐고, 실험 횟수를 40회 이상으로 끌어올렸습니다.

두 번째는 파편화된 테스트 파일의 스케일 왜곡 문제였습니다. 파일 10개가 서로 다른 시점의 데이터를 담고 있어, 개별 정규화 시 비수기 최대 매출과 성수기 최대 매출이 모두 1.0으로 변환되는 왜곡이 발생했습니다. train 전 기간에서 메뉴별 진짜 최댓값·최솟값을 추출해 고정 글로벌 스케일러를 구축하고, 10개 파일 전체에 동일 기준을 적용해 모델이 체급 차이를 왜곡 없이 학습하도록 설계했습니다.

그 결과 RMSE를 18.45에서 8.12로 줄여 오차를 55.9% 개선했고, 상위 3.8%(Top 12)로 대회를 마무리했습니다. 생산 공정 데이터도 설비·라인·시점별로 파편화되어 있어, 이 전처리·모델링 역량을 공정 이상 감지와 부품 수요 예측에 직접 활용할 수 있습니다.
